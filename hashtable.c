#include <studio.h>

struct pair{
    int value;
    int key;
};

void main()
{
    int size, i;
    printf("enter the size of the table");
    scanf("%d", &size);
    struct pair hash_table[size];
    printf("Enter the elements: ")
    for (i=0; i<size; i++)
    {
        scanf("%d", &temp )
        hash_table[]
    }
}